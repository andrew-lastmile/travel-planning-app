{
  "buildCommand": "npm install --prefix frontend && npm run --prefix frontend vercel-build && rm -rf dist && cp -R frontend/dist dist",
  "outputDirectory": "dist",
  "framework": null,
  "functions": {
    "api/*.js": {
      "maxDuration": 10
    }
  }
}
